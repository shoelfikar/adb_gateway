run:
  timeout: 5m
  go: "1.24"

linters:
  enable:
    - errcheck
    - gosec
    - bodyclose
    - revive
    - govet
    - staticcheck

issues:
  exclude-rules:
    - path: _test.go
      linters:
        - errcheck
        - gosec